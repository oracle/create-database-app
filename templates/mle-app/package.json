{
  "name": "<%= appName %>",
  "version": "1.0.0",
  "devDependencies": {
    "mle-js": "^23.7.0",
    "typescript": "^5.7.3",
    "esbuild": "0.25.1"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify=false --platform=neutral --format=esm --outfile=dist/index.js",
    "deploy": "node deploy.js",
    "initdb": "node src/database/db.js src/database/initdb.sql",
    "cleandb": "node src/database/db.js src/database/cleanup.sql"
  }
}